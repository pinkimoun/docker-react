sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t pinkimoun/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pinkimoun/docker-react npm run test
